{
  "build": {
    "builder": "python"
  },
  "deploy": {
    "startCommand": "gunicorn mentorcraft.wsgi",
    "installCommand": "pip install -r requirements.txt && python manage.py migrate && python manage.py collectstatic --noinput"
  }
}
